export enum SelectedPage {
    Home = 'home',
    Products = 'products',
    Features = 'features',
    About = 'about',
    Contact = 'contact'
  }
export interface StatisticType {
    icon: JSX.Element;
    title: string;
    description: string;
  }